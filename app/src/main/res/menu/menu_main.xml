<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".activity.GameActivity">

    <item
        android:id="@+id/menu_life_item"
        android:title="@string/menu_life_title"
        android:icon="@drawable/ic_love"
        app:actionLayout="@layout/menu_life"
        app:showAsAction = "always"
        android:orderInCategory="1"
        />
    <item
        android:id="@+id/menu_score_item"
        android:title="@string/menu_score_title"
        android:icon="@drawable/ic_score"
        app:actionLayout="@layout/menu_score"
        app:showAsAction = "always"
        android:orderInCategory="2"
        />

</menu>